vector<string> stringMatching(vector<string>& words) {
        vector<string> ch;
        for(int i=0;i<words.size();i++){
            for(int j=0;j<words.size();j++){
                if(words[j].find(words[i]) !=-1 and words[i]!=words[j]){
                    ch.push_back(words[i]);
                    break;
                }
                
            }
        }
        return ch;
    }
